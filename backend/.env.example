# Configuración del servidor
PORT=5000
NODE_ENV=development

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=local_bands_map
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña

# JWT
JWT_SECRET=tu_clave_secreta_muy_segura_cambiar_en_produccion
JWT_EXPIRES_IN=7d

# CORS (separar múltiples URLs con comas para desarrollo y producción)
FRONTEND_URL=http://localhost:5173,https://tu-dominio.vercel.app

# Spotify API
SPOTIFY_CLIENT_ID=tu_spotify_client_id
SPOTIFY_CLIENT_SECRET=tu_spotify_client_secret

# YouTube Data API v3 (OPCIONAL - la app funciona sin esto)
# Obtén tu API key en: https://console.cloud.google.com/
YOUTUBE_API_KEY=
